# NOTE: make sure to update the requirements-frozen.txt file when making a change here
# Sort this file lexicographically
# Generate the requirements-frozen.txt file as follows:
#  virtualenv -p python3.6 pip_frozen
#  source pip_frozen/bin/activate
#  pip install -r requirements.txt
#  pip freeze > requirements-frozen.txt
#  deactivate
#  rm -rf pip_frozen
# add in any extra lines from the end of this file
# diff requirements.txt and requirements-frozen.txt to see that requirements-frozen.txt is not overriding any
# selections in requirements.txt
beakerx==0.16.1
jupyter-client==5.2.3
nbconvert==5.6.0
nbformat==4.4.0
numpy==1.15.3
pygit2==0.28.2
pytest==5.2.0
pytest-timeout==1.3.3
pytest-xdist==1.30.0
python-dateutil==2.8.1
requests==2.20.0
retrying==1.3.3
tornado==5.1.1
# extra requirements that will not be generated by pip freeze and need to be added to requirements-frozen.txt
pip==9.0.1; python_version >= '3.6'
file:../cli#egg=cook_client
